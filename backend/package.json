{
  "name": "backend",
  "version": "1.0.0",
  "description": "Python backend",
  "scripts": {
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "@prettier/plugin-python": "^1.0.0"
  }
}
