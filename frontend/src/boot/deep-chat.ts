/**
 * Deep Chat Boot File
 * Configures Deep Chat custom element for Vue 3
 */

import { boot } from 'quasar/wrappers';
import 'deep-chat';

export default boot(() => {
  // Deep Chat is configured as a custom element in quasar.config.ts
  // No runtime configuration needed here
});
